global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'taiji-monitor'

scrape_configs:
  - job_name: 'taiji'
    static_configs:
      - targets: ['taiji:8000']
    metrics_path: '/metrics'
    scrape_interval: 5s